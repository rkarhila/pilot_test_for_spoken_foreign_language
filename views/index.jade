extends layout

block content
  h1 Puhutun ruotsin kokeen pilotti
  p Olet 
      strong= username 
      |  (<i>
      = user.fullname
      | </i>), ja roolisi on 
      strong= user.role 
      -if (user.school)
          |  koulussa 
          strong= user.school
          | . 
      a(href="logout") Kirjaudu ulos.


  // Wrapper
  #wrapper
      h2  #[a(href='#{base_url}/users') Käyttäjähallinta]
      h2 #[a(href='#{base_url}/test') Tehtävät]
      h2  Lataa data omalle koneelle 
      //h2 #[a(href='#{base_url}/datadump') Lataa data omalle koneelle]

  // /WRAPPER